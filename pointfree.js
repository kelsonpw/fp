// Pointfree programming

const array = [1, 2, 3];
const double = x => x * 2;
const doubled = array.map(double);
console.log(doubled);
// Legibility
// Reduce surface area for bugs
// Unit test named fns
